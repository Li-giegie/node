package message

import (
	"testing"
)

func TestMarshalAndUnmarshalAuthReq(t *testing.T) {
	//m := newMessage(1, 2)
	//req := newAuthReq(m)
	//buf, err := req.marshal([]byte("hello"))
	//if err != nil {
	//	t.Error(err)
	//	return
	//}
	//fmt.Println(buf)
	//resp := new(authReq)
	//resp.init(m)
	//if err = resp.unmarshal(buf); err != nil {
	//	t.Error(err)
	//	return
	//}
	//fmt.Println(resp.version, resp.checksum, string(resp.data), resp.getType(), resp.getSrcId(), resp.getDstId())
}
